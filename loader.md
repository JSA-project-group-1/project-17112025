# 📘 Local Loader — Документация для команды

Этот документ описывает использование локального (секционного) Loader, который работает в любой части проекта и не мешает другим разработчикам.

## 🔥 Что такое локальный лоадер?

### Локальный лоадер — это универсальный компонент, который:

- появляется только внутри выбранного контейнера,

- перекрывает только эту секцию, а не весь экран,

- автоматически создаётся и исчезает,

- не требует ручного HTML,

- не конфликтует с другими блоками.

### Используется, когда секция делает асинхронную работу: 

- API-запросы,

- фильтрация,

- пагинация,

- подгрузка карточек,

- рендер списков,

- обновление части страницы.


## 📁 Структура проекта
```
src/
  js/
    main-home.js
    loader.js            ← функционал лоадера
    pixabay-api.js
    render-functions.js

  css/
    loader.css           ← стили лоадера
    pages/
      home.css

  partials/
    block-categories-list.html
    ...
```
## ✔ Разработчикам НЕ нужно ничего подключать вручную

Лоадер уже импортирован в общий код.

### 🧩 1. Как подключён лоадер (уже сделано)

``` 
В main-home.js:

import "../css/loader.css";
import { showLoader, hideLoader } from "./loader.js";
```

Благодаря этому лоадер доступен в любом модуле проекта.

### 🧩 2. Как должен выглядеть контейнер секции

Пример из block-categories-list.html:
```
<div class="block-categories" id="categories-section">
  <ul class="block-categories-list"></ul>
  <ul class="exercises-list"></ul>
  <ul class="pagination-controls-list"></ul>
</div>
```
## ⚠ Обязательно:
```
Каждый контейнер, где вы хотите видеть лоадер, должен иметь ID.
```
### 🧩 3. Как использовать лоадер в своей секции

Каждый разработчик выполняет ТРИ шага.

1. ✔ ШАГ 1 — Дайте контейнеру ID
```
<section class="hero" id="hero-section"></section>

<div id="equipment-section"></div>
```
2. ✔ ШАГ 2 — Получите контейнер в JS
```
const heroSection = document.getElementById("hero-section");
```

3. ✔ ШАГ 3 — Покажите и спрячьте лоадер внутри async-функции
```
async function loadHeroData() {
  showLoader(heroSection);

  try {
    const data = await fetchHero();
    renderHero(data);
  } catch (e) {
    console.error(e);
  } finally {
    hideLoader(heroSection);
  }
}
```

## 🎉 Всё. Больше ничего делать не нужно.

### 🧩 4. Пример интеграции из реального проекта
```
async function loadAndRenderCategoriesList() {
  categoriesList.innerHTML = '';
  exercisesList.innerHTML = '';
  paginationList.innerHTML = '';

  showLoader(categoriesSection);

  try {
    await new Promise(resolve => setTimeout(resolve, 2000)); // Визуальная задержка лоадера (чтоб видеть что работает. Можно убрать)

    const categories = await fetchCategories(
      currentFilter,
      currentPage,
      categoriesPerPage
    );

    const { results, page, totalPages } = categories;

    renderCategories(results, categoriesList);
    renderPagination(Number(totalPages), Number(page), paginationList);

  } catch (error) {
    warningP.classList.remove('visually-hidden');
    iziToast.error({
      icon: '',
      position: 'topRight',
      message: error.message,
    });
  } finally {
    hideLoader(categoriesSection);
  }
}
``` 

### 🧩 5. Стили лоадера (loader.css)

```
.local-loader-wrapper {
  position: absolute;
  inset: 0;
  background: rgba(255, 255, 255, 0.6);
  display: flex;
  align-items: center;
  justify-content: center;
  z-index: 10;
}

.local-loader-wrapper.hidden {
  display: none;
}

.hypnotic {
  width: 67.2px;
  height: 28px;  border: 2.2px solid #eea10c;
  border-radius: 50%;
  display: grid;  animation: hypnotic-spin 2s infinite linear;
}

.hypnotic:before,
.hypnotic:after {
  content: "";
  grid-area: 1/1;
  border: inherit;
  border-radius: 50%;
  animation: inherit;
  animation-duration: 3s;
}

.hypnotic:after {
  --s: -1;
}

@keyframes hypnotic-spin {
  100% {
    transform: rotate(calc(var(--s, 1) * 1turn));
  }
}
``` 

Что происходит внутри:

- ▶ Создание лоадера при первом вызове

- ▶ Центрирование внутри секции

- ▶ Перекрытие только выбранного контейнера

- ▶ Скрытие при повторном вызове без удаления DOM-узла

### 🧩 7. FAQ — ответы на частые вопросы
```
Вопрос	                                        Ответ
Лоадер перекрывает всю страницу?	        ❌ Нет. Только выбранную секцию.
Можно ли несколько лоадеров одновременно?	✔ Да, абсолютно.
Нужно ли вставлять HTML?	                ❌ Нет, он генерируется JS-кодом.
Нужно ли что-то подключать руками?	        ❌ Нет, всё уже импортировано.
Можно менять цвет/размер/скорость?	        ✔ Да, в loader.css.
```
### 🧩 8. Мини-шаблон для разработчика
```
import { showLoader, hideLoader } from "./loader.js";

const section = document.getElementById("SECTION-ID");

async function loadData() {
  showLoader(section);

  try {
    const data = await fetch();
    render(data);
  } finally {
    hideLoader(section);
  }
}
```

## 🎯 Итог

### Этот компонент:

- полностью автономен,

- универсален,

- не ломает разметку,

- интегрируется за 3 шага,

- подходит для любых секций,

- не создаёт конфликтов в группе разработчиков.